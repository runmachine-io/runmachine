syntax = "proto3";

package runm;

// An object type is a simple classification for various types of things known
// to the runm system
message ObjectType {
    string code = 1;
    string description = 2;
}
