syntax = "proto3";

package runm;

import "wrappers.proto";

// A binary capability tag to decorate providers
message Trait {
    string code = 1;
    StringValue description = 2;
    uint32 generation = 100;
}
